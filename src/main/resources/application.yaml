spring:
  application:
    name: ai-coding-study
# springdoc-openapi
springdoc:
  group-configs:
    - group: 'default'
      paths-to-match: '/**'
      packages-to-scan: com.zlj.aicodingstudy.controller
# knife4j
knife4j:
  enable: true
  setting:
    language: zh_cn
